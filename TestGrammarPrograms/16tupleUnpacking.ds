// global declaration of tuple

tuple(real, integer, boolean) rib_tup = (2.1, 3, true);

{
  real r = 0;
  integer i = 0;
  boolean b = false;

  // tuple unpacking
  r,i,b = rib_tup;


  // Swapping syntax
  r,i = (i,r);
}