tuple(integer i, real r, boolean b) tuple_var;
tuple_var = (1, 0.2, true);

const tuple(integer, real r, boolean) tuple_var = (111, 1.2, false);

var tuple(integer i, real, boolean b) tuple_var;
tuple_var = (11, 0.2, true);

tuple_var.1 = 0;
a_.21 = true;
t._ = 2;
i._ad_ = 5;

tuple(real, integer) ri_tup = (2.1, 3);
tuple(real, real) rr_tup = (2.1, 3);  /* rr_tup == (2.1, 3.0) as the integer is promoted */

const boolean b = ri_tup == rr_tup; /* b == true */