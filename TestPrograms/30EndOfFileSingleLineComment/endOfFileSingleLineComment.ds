const x = 1; // var y = 2;

procedure main() returns integer {
	/* var z = 42; */
	
	/* This is a block comment
	 * and 
	 
	 const y = 3;
	 
	 * should not display
	ada faf */
	
	const z = 4; 
	return z;
}

// end of line comment should not throw parser error