//8.5.4 Operations: + [a, b] = [a, b] (nop)

const out = std_output();

procedure main() returns integer {
	integer interval a = -1..2;
	integer interval b = +a;
	
	integer vector c = b;
	
	c[1] -> out; '\n' -> out;	
	c[2] -> out; '\n' -> out;	
	c[3] -> out; '\n' -> out;	
	c[4] -> out;
	
	return 0;
}