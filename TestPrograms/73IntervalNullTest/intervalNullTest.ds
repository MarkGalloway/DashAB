// 8.5.2 A null valued interval is defined as null..null, and it is always 0..0 because DashAB only supports integer values.

const out = std_output();

procedure main() returns integer {
	integer interval i = null..null;
	integer vector v = i;
	
	v[1] -> out;
	
	return 0;
}