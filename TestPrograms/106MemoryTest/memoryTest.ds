const out = std_output();

procedure main() returns integer {
	integer interval i1 = 1..5;
	var i = 0;
	
	loop while i < 5 {
		integer interval i2 = 1..5;
		
		if (i > 4)
			integer interval i3 = 2..3;
		
		if (i > 3)
			return 0;
			
		if (i > 2)
			break;
			
		i1 = i..(i+5);
		i2 = (i+5)..(i+10);
		
		i2 -> out; '\n' -> out;
		
		i = i + 1;
	}
	
	i1 -> out; '\n' -> out;
	
	return 0;
}