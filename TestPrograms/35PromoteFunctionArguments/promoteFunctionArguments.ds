const out = std_output();

function squared(real a) returns real {
    return a*a;
}

procedure main() returns integer {
    integer c = 5;
    
    squared(5) -> out; '\n' -> out;
    squared(c) -> out ; '\n' -> out;
    
    return 0;
}